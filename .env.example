# Postgres connection for the admin dashboard database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=ruko_admin

# Optional tuning
POSTGRES_CONNECT_TIMEOUT=5
DB_POOL_MIN=1
DB_POOL_MAX=10

# App behavior
ADMIN_DB_INIT_ON_STARTUP=0
PORT=8080
